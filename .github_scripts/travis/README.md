To install into repo:

```
git subtree add --prefix .github_scripts https://github.com/VoronDesign/GithubScripts.git master --squash
ln -s ./.github_scripts/travis/.travis.yml .travis.yml
```

Then commit.
